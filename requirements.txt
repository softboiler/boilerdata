# List project dependencies here. A workflow ports them to `pyproject.toml`. This is needed because Dependabot does not currently support `flit`-flavored `pyproject.toml`.
dill==0.3.6
IPython[notebook]==8.14.0
matplotlib==3.7.1
numpy==1.25.0
pandas==2.0.2
pandera==0.15.1
papermill==2.4.0
propshop==0.1.1
pyarrow==12.0.1
pydantic==1.10.9
pyjanitor==0.24.0
pyXSteam==0.4.9
pyyaml==6.0
ruamel.yaml==0.17.32
seaborn==0.12.2
scipy==1.11.1
sympy==1.12
uncertainties==3.1.7

# ? Issue upgrading to dvc>=3.
# ? Would need to `dvc gc --cloud ...` and dump everything prior to DVC 3.
# ? This is because links cannot be made between the cache files so it is forcibly
# ? duplicated.
dvc[gs]==2.58.2
# ? https://github.com/nteract/papermill/issues/711
# ? When the below PR is merged, this should be fixed
# ? https://github.com/nteract/papermill/pull/726
jupyter-client==7.4.9

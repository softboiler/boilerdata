{
  "title": "Columns",
  "description": "Configuration for a column after Migration 3.",
  "type": "object",
  "properties": {
    "columns": {
      "title": "Columns",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/Column"
      }
    }
  },
  "required": [
    "columns"
  ],
  "definitions": {
    "Column": {
      "title": "Column",
      "description": "Configuration for a column after Migration 3.",
      "type": "object",
      "properties": {
        "label": {
          "title": "Label",
          "description": "The column name.",
          "default": "...",
          "type": "string"
        },
        "units": {
          "title": "Units",
          "description": "The units for this column's values.",
          "default": "...",
          "type": "string"
        }
      }
    }
  }
}

{
  "title": "MyModel",
  "description": "Docstring for MyModel",
  "type": "object",
  "properties": {
    "my_enum": {
      "description": "Description for this field.",
      "allOf": [
        {
          "$ref": "#/definitions/MyEnum"
        }
      ]
    }
  },
  "required": [
    "my_enum"
  ],
  "definitions": {
    "MyEnum": {
      "title": "MyEnum",
      "description": "Docstring for MyEnum.",
      "enum": [
        "T1",
        "T2",
        "T3"
      ]
    }
  }
}
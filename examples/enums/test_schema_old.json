{
  "title": "MyModel",
  "description": "Docstring for MyModel.",
  "type": "object",
  "properties": {
    "my_list": {
      "title": "My List",
      "description": "Description for my_list.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/EnumModel"
      }
    }
  },
  "required": [
    "my_list"
  ],
  "definitions": {
    "MyEnum": {
      "title": "MyEnum",
      "description": "Docstring for MyEnum.",
      "enum": [
        "T1",
        "T2",
        "T3"
      ]
    },
    "EnumModel": {
      "title": "EnumModel",
      "description": "Docstring for EnumModel.",
      "type": "object",
      "properties": {
        "my_enum": {
          "description": "Description for my_enum.",
          "allOf": [
            {
              "$ref": "#/definitions/MyEnum"
            }
          ]
        }
      },
      "required": [
        "my_enum"
      ]
    }
  }
}

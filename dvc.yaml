stages:
  pipeline:
    cmd: "python -m boilerdata.pipeline"
    deps:
      - "data/curves"
    params:
      - "config/axes.yaml":
      - "config/project.yaml":
      - "config/trials.yaml":
    outs: ["data/results"]

  plot:
    cmd: "python -m boilerdata.plot"
    deps:
      - "data/plotter"
      - "data/results"
    plots: ["data/plots/lit_.png"]
